# Historical strategy locks

These JSON files preserve locally dated parameters from the original research process.
Their positive descriptions are historical context, not current verdicts. The locks are
not independently timestamped, and every evaluation tied to the v2 episodes is a
source-time counterfactual.

For current conclusions use `artifacts/publication_claims.json` and
`artifacts/audit_v2/selection_audit.json`.
