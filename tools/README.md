# Tools

101 research scripts. Each is runnable standalone with `py tools/<name>.py`;
most accept `--help`. Descriptions come from each script's docstring and are
regenerated by `tools/generate_script_index.py`.

Start here:

- `backtest_locked_strategy.py` — historical runner; required data/model artifacts are not public
- `audit_strategy_snooping.py` — the anti-snooping audit that killed the strategy (gate D2)
- `audit_execution_realism.py` — historical invalid execution audit; descriptive output only
- `build_btc_5m_episode_dataset.py` — guarded source-time counterfactual builder; not causal/live-achievable

## Dataset construction

| Script | Purpose |
|---|---|
| `augment_dense_close_no_bias.py` | Augment the dense_close parquet with RAW-binance-derived versions of |
| `build_brier_table.py` | Build the canonical Brier comparison from the committed TCN seed reports. |
| `build_btc_5m_episode_dataset.py` | Build historical BTC binary-market tensors from source-time replay. |
| `build_dataset_window.py` | Build a dense_close parquet for an arbitrary [start, end] UTC window. |
| `build_dense_close_only.py` | Build ONLY the dense_close dataset for one or more UTC days. |
| `build_fair_value_dataset.py` | Build the fair_value_v1 training dataset (Polymarket L2 + a CEX spot feed + RTDS). |
| `build_fair_value_v2_source_time.py` | Build fair_value_v2_source_time from raw source/event clocks. |
| `build_poly_l2_last60s.py` | Build the polymarket_l2_last60s_v1 training dataset. |
| `build_public_paper.py` | Build the corrected public-evidence PDF from its manuscript and claim register. |
| `build_publication_claims.py` | Render the canonical public claim register to Markdown. |
| `build_sequence_dataset_from_fv.py` | Build v0 market-sequence tensors from fair_value_v2 source-time snapshots. |
| `build_splits_v2.py` | Assemble dataset v2 splits from existing daily episode shards (Phase 3). |
| `cache_synthetic_chainlink_frame.py` | Materialise and cache the synthetic Chainlink reference frame for a day range. |
| `make_tcn_retrain_bundle.py` | Pack the TCN v2 retrain bundle for Colab (Phase 4). |

## Backtesting and replay

| Script | Purpose |
|---|---|
| `backtest_fair_value_delay_window.py` | Backtest fair_value_v1 with an execution delay. |
| `backtest_fair_value_ttc.py` | Backtest the fair_value_v1 model over DIFFERENT ttc entry windows. |
| `backtest_live_config.py` | Quick backtest of the CURRENT live config on OOS days: |
| `backtest_live_days_byside.py` | Backtest the fair_value_v1 model on the LIVE days (06-25/26/27), broken down |
| `backtest_locked_strategy.py` | Backtest a locked strategy JSON through the canonical shared engine. |
| `backtest_maker_oos.py` | OOS maker-fill backtest for model 02. |
| `backtest_maker_threshold_sweep.py` | Fine-grained sweep of edge_threshold for the maker (POST_ONLY at midpoint) |
| `backtest_maker_with_floor.py` | Re-run the maker OOS threshold sweep with MIN_DOWN_PRICE = 0.30 |
| `backtest_poly_l2_only_bucketed_mp.py` | Bucketed backtest (multiprocessing variant) — generic over v1/v2 models. |
| `backtest_poly_l2_only_v1_bucketed.py` | Bucketed backtest of poly_l2_only_v1. |
| `backtest_poly_l2_only_v1_quick.py` | Quick backtest of poly_l2_only_v1: $5 per market, one entry per market, |
| `backtest_taker_no_bias.py` | Re-evaluate the no-bias calibrated model as a TAKER execution model |
| `calib2d_ev_backtest.py` | 2D calibration (TTC x price) + EV-oriented threshold, evaluated from the |
| `eval_v2_seed_policies.py` | Seed-variance evaluation of the v2 retrained TCNs on policy PnL (Phase 4.2/5). |
| `score_shadow_pnl.py` | Score realized PnL of TCN shadow-bot entries against market resolutions. |

## Audits and verification

| Script | Purpose |
|---|---|
| `audit_btc_5m_episodes.py` | Strict audit of data/datasets/btc_5m_episodes_v1_200ms. |
| `audit_btc_5m_episodes_summary.py` | Finalize the episode-dataset audit: enrich audit_report.json with caveats / |
| `audit_execution_realism.py` | Depth-aware execution realism audit for shadow ENTER decisions (Phase 2.3). |
| `audit_strategy_snooping.py` | Anti-snooping audit of the locked TCN double strategy (Phase 2.1 + 2.2). |
| `check_recorder_liveness.py` | Recorder/shadow-bot liveness check. |
| `diag_book_parity.py` | Decisive book-parity test: replay the RECORDER PM-L2 up to each live ENTER's |
| `diagnose_order.py` | Try to submit ONE small FAK order with explicit options to find out |
| `diagnose_v2_transfer.py` | Diagnose why the v2 ensemble under-trades v1 thresholds (Plan v2.1, Phase 1). |
| `validate_rtds_vs_resolution.py` | Validate that the PUBLIC Polymarket RTDS Chainlink feed |
| `verify_book_parity.py` | Verify the LIVE book is actually correct — the REAL test that mid_sum=1.0 |
| `verify_fv_decision_parity.py` | Verify fair-value live ENTER decisions against an exact raw-data replay. |
| `verify_tcn_shadow_parity.py` | Verify TCN shadow decisions against a source-time daily episode replay. |

## Policy search and selection

| Script | Purpose |
|---|---|
| `compare_exit_policies.py` | Hold vs stop-loss exits, risk-normalized (Phase 5.3). |
| `deep_search_fair_value_v2_policies.py` | Deep policy search for fair_value_v2_source_time model outputs. |
| `drilldown_refined_rule.py` | Day-by-day, price-bucket-by-price-bucket drilldown of the refined rule: |
| `search_consistent_strategy.py` | Search for a CONSISTENT strategy (smooth up-curve), guarding against |
| `search_fair_value_v2_policies.py` | Search source-time fair-value policies without retraining the model. |
| `search_policy_walkforward.py` | Walk-forward policy re-derivation over the regenerated search universe (Phase 5.1). |
| `search_reentry_strategy.py` | Develop the bucket strategy with PRICE-DEPENDENT re-entry risk rules. |
| `search_robust_strategy.py` | Search for a strategy whose max drawdown is SMALL relative to its PnL |
| `search_sequence_policy.py` | Search trading policies over market/LGBM/sequence episode predictions. |
| `search_tcn_exit_policy.py` | Search active-exit policies for the best TCN entry strategy. |
| `search_tcn_fullmarket_exit_policy.py` | Explore TCN entry+exit policies outside the original TTC 15-90 window. |
| `search_v2_1_policies.py` | Search v2.1: calibrated EV + EV-proportional sizing + portfolio (Plan v2.1, Phase 2). |
| `search_v2_2_policies.py` | Search v2.2: constant-base sizing (5 shares, boost to 7.5 on big edge) with |

## Analysis and diagnostics

| Script | Purpose |
|---|---|
| `analyze_bucket_strategy.py` | Develop the one-entry-per-TTC-bucket strategy: break down PnL by bucket, and |
| `analyze_chainlink_public_delay.py` | Measure the public delayed Chainlink page's lag against CEX spot, by hour and regime. |
| `analyze_crash_delay.py` | For the final strategy entries, look at the 2-second delayed fill price and |
| `analyze_delta_to_strike_chainlink_error.py` | Quantify the delta-to-strike error introduced by using a Chainlink proxy instead of the settlement feed. |
| `analyze_l2_audit.py` | Compare direct-WS L2 audit events against recorder L2 events. |
| `collect_signals_l2_only.py` | Phase 1: walk every market across N OOS days, sample the model on a |
| `compute_live_bias.py` | Compute the live_reference_events bias at a given moment WITHOUT rebuilding |
| `diff_live_vs_backtest.py` | Diff the live bot's decisions on date D against what the backtest |
| `dump_consistent_trades.py` | Dump per-trade timeline (datetime, side, fill, pnl, cumulative equity) for the |
| `dump_live_features.py` | Read-only live feature diagnostic. Connects to the Polymarket market WS the |
| `dump_one_per_market.py` | Same consistent config (thr=0.15, fill[0.25,0.60]) but ONE entry per market |
| `feature_divergence.py` | Rank EVERY model feature by how much the LIVE bot's value diverges from the |

## Operations

| Script | Purpose |
|---|---|
| `bootstrap_polymarket.py` | One-time Polymarket wallet bootstrap. |
| `canonical_daemon.py` | Lightweight background loop that keeps today's canonical |
| `experiment_synthetic_chainlink.py` | Sweep synthetic-Chainlink construction parameters and score each against recorded resolution prices. |
| `nightly_shadow_ops.py` | Nightly shadow-evidence pipeline (Phase 0.4 + 3.2 ops). |

## Other

| Script | Purpose |
|---|---|
| `fit_and_backtest_calibrated.py` | Fit an isotonic-regression calibrator on the model's test split, then |
| `grid_search_v2_policies.py` | Policy grid search for the v2 retrained TCNs — snooping-safe protocol (Phase 5). |
| `inspect_maker_fills.py` | Inspect every fill from the maker-OOS backtest at a specific threshold. |
| `leakage_audit.py` | Forensic leakage audit on a specific suspicious fill. |
| `live_feature_parity.py` | Live-pipeline feature parity test. |
| `refine_entry_rules.py` | Analyse the no-bias model's OOS fills to find tight entry rules that |
| `report_poly_l2_last60s.py` | Sanity report over data/datasets/polymarket_l2_last60s_v1/*.parquet. |
| `repository_stats.py` | Print current repository counts without publishing stale hard-coded numbers. |
| `research_chainlink_proxy_v2.py` | Research the v2 Chainlink proxy: candidate source blends, lag alignment, and residual error. |
| `risk_profile_iso_global.py` | Risk profile of the winning config from calib2d_ev_backtest: |
| `risk_profile_no_bias.py` | Deep risk-profile analysis of the no-bias model on OOS test data. |
| `rtds_coverage_and_chainlink_compare.py` | (1) RTDS coverage across all available raw days, and |
| `save_strategy_calibrator.py` | Fit the strategy's isotonic calibrator on the OOS calib days (04-22, 04-30) |
| `shadow_parity_diff.py` | Compare a shadow JSONL log against a reference parquet of the same |
| `shadow_sweep.py` | Run the shadow decision pipeline across many existing dense_close |
| `sweep_strategy_hour_of_day.py` | Hour-of-day strategy analysis. |
| `sweep_strategy_hybrid.py` | Hybrid strategy sweep — allows different side rules in different TTC zones. |
| `sweep_strategy_l2_only.py` | Phase 2: load Phase 1 signals, sweep (TTC window × side) strategies under |
| `sweep_strategy_tiered_edge.py` | Tiered-edge strategy sweep. |
| `test_entry_delay.py` | Execution-latency stress test of the FINAL strategy |
| `test_maker_sub_dollar.py` | Wire-test: send a deliberately sub-$1 POST_ONLY order so Polymarket |
| `train_episode_baselines.py` | Train baseline models for the 5Hz BTC 5m episode dataset. |
| `train_episode_gru.py` | Train a residual GRU on the 5Hz BTC episode dataset. |
| `train_episode_patch_transformer.py` | Train a causal Patch Transformer on the BTC 5m episode dataset. |
| `train_episode_tcn.py` | Train a causal residual TCN on the 5Hz BTC episode dataset. |
| `train_fair_value_v1.py` | Train the fair_value_v1 model and forward-walk evaluate it. |
| `train_fair_value_v2_source_time.py` | Train and execution-backtest fair_value_v2_source_time. |
| `train_no_bias_model.py` | Train a fresh LightGBM on the no-bias dataset with: |
| `train_poly_l2_only_v1.py` | Train poly_l2_only_v1 — a LightGBM binary classifier on the |
| `train_poly_l2_only_v2.py` | Train poly_l2_only_v2 — residual-target LightGBM. |
| `train_sequence_v0.py` | Quick residual sequence-model sanity check for btc_5m_sequences_v0_from_fv. |
